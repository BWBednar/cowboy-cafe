<UserControl x:Class="PointOfSale.OrderControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PointOfSale"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="100"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1000"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <local:MenuItemSelectionControl Grid.Row="1" Grid.Column="0" Width="1000"/>
            <local:OrderSummaryControl Grid.Row="1" Grid.Column="1"/>

        

            <Button Name="ItemSelectionButton" Grid.Column="0" Height="75" FontSize="30">Item Selection</Button>
            <Button Name="CancelOrderButton" Grid.Column="1" Height="75" FontSize="30">Cancel Order</Button>
            <Button Name="CompleteOrderButton" Grid.Column="2" Height="75" FontSize="30">Complete Order</Button>
        </Grid>
    
</UserControl>
